# Secrets
.env
.env.*
!.env.example
!.env.sample
*.pem
*.key

# Claude Code local settings (machine-specific permissions) and worktrees
# (isolated copies for background agents -- transient, never project source)
.claude/settings.local.json
.claude/settings.local.json.bak
.claude/settings.local.json.*.bak
.claude/worktrees/
.claude/scheduled_tasks.lock
*.bak

# Per-project ForgeRouter credentials (see host-bridge/app.py)
.codex/forgerouter.env
.forgerouter/antigravity.env

# Python
.venv/
__pycache__/
*.py[cod]
.pytest_cache/

# Node / frontend build
node_modules/
dist/
*.tsbuildinfo

# OS / editor
.DS_Store
*.swp

# dev.sh state (pidfiles + logs for the local dev servers)
.dev/

# Superdesign temporary component templates
.superdesign/tmp/
